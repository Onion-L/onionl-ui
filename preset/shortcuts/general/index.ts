import type { Shortcut } from 'unocss'

export const generalShortcuts: Shortcut[] = [
  ['is-disabled', '!cursor-not-allowed [filter:opacity(0.5)]'],
  ['is-center', 'flex justify-center items-center'],
]
