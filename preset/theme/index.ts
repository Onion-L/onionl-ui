import type { Theme } from '@unocss/preset-mini'
import { colors } from './colors'

export const theme: Theme = {
  colors,
  borderRadius: {
    'circle': '50%',
    'button-square': '8px',
    'square': '4px',
  },
}
